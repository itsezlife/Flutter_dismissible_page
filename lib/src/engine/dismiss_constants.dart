/// Default drag progress (0.0–1.0) at which a gesture counts as a dismissal.
const double kDismissThreshold = 0.15;

/// Default quiet interval after user paging before pager-axis edge dismiss
/// re-arms.
const Duration kEdgeDismissCooldown = Duration(milliseconds: 300);
